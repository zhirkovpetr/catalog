import preloader from "../../assets/images/gif/preloader.gif";

const Preloader = () => {
  return (<div>
    <img alt={'LOADING, PLEASE WAIT'} src={preloader} />
  </div>)
}

export default Preloader;
